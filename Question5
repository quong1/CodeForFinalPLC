/*Java Code without gotos*/
//Create a boolean to return true if all-zero row is found 

boolean bool= false;
for(i = 1; i <= n; i++) {
//Create counter for all-zero row
  int counter = 0;
    for(j = 1; j <= n; j++) {
      if(x[i][j] == 0)
        counter++;
    } 
    //if counter equals number of rowsand flag is false, the first all-zero row is found
    if(count == n && flag == false){
      System.out.println("First all-zero row is:" + i);
      flag = true;
    }
}
